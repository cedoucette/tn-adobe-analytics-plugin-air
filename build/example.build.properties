

# Must be declared as <property> in build.xml
# LOCALE = n/a



##
# COMPILER
##

MXMLC.JAR	= ${FLEX_HOME}/lib/mxmlc.jar
ADL			= ${FLEX_HOME}/bin/adl
ADT.JAR		= ${FLEX_HOME}/lib/adt.jar
ADT			= ${FLEX_HOME}/bin/adt.bat

DEBUG		= true
OPTIMIZE	= false
COMPRESS	= false
STORETYPE	= pkcs12

flexTasks=${FLEX_HOME}/ant/lib/flexTasks.jar



##
# ASSETS
##

# Paths de l'app
platform.app.basedir	= ${basedir}
platform.app.src		= ${basedir}/src
platform.app.buildDir	= ${basedir}/build/irw



# Paths d'assets common IRW
# irwcommons.app.basedir	= ../../tn-irw-common-assets

# Used to include brand-specific assets, e.g.:
# irwcommons.app.brand				= caillou



build.root.path			= ${basedir}
build.dir				= ${platform.app.buildDir}
dev.dir					= ${platform.app.src}

# iconFolder.light	= ${basedir}\\resources\\icons
# iconFolder.full		= ${basedir}\\resources-full\\icons



##
# CERTIFICATES INFORMATION
# 
# Note: Ant script temporarily copies all certificate files into each project's directory due
# 		to permissions, and deletes them once the build completes. This is why paths below are
#		specified as ${basedir}/build/ even though the files themselves are missing.
##


# Path to certificates
cert.src = ../../tn-irw-build-common/cert

# Certificate information
cert.ios.type = pkcs12
cert.password = 2yBdkrxCxmdc

# ipa-ad-hoc ipa-app-store ipa-debug ipa-test ipa-debug-interpreter ipa-test-interpreter ipa-test-interpreter-simulator
IPA_TYPE.DEV			= ipa-ad-hoc
IPA_TYPE.DEBUG			= ipa-debug-interpreter
IPA_TYPE.QA				= ipa-ad-hoc
IPA_TYPE.QA-Lite		= ipa-ad-hoc
IPA_TYPE.APPSTORE		= ipa-app-store
IPA_TYPE.APPSTORE-Lite	= ipa-app-store
IPA_TYPE.DESKTOP		= air


# Provisioning information
#
# Dev/debugging
provisioning.debug.cert.ios					= ${basedir}/build/cert/ios_distribution_in_house.p12
provisioning.debug.cert.selfSigned			= ${basedir}/build/cert/selfSigned.p12
provisioning.debug.profile					= ${basedir}/build/cert/TN_In_House.mobileprovision
provisioning.debug.passphrase				= 2yBdkrxCxmdc
provisioning.debug.selfSigned.passphrase	= test

# Release
provisioning.release.cert.ios		= ${basedir}/build/cert/tn_app_store_distribution.p12
provisioning.release.passphrase		= 2yBdkrxCxmdc
# provisioning.release.profile.lite	= [Located in project's build.properties]
# provisioning.release.profile.full	= [Located in project's build.properties]



provisioning_profile.DEV		= ${provisioning.debug.profile}
cert.ios.DEV					= ${provisioning.debug.cert.ios}
cert.ios.DEV.passphrase			= ${provisioning.debug.passphrase}

provisioning_profile.DEBUG		= ${provisioning.debug.profile}
cert.ios.DEBUG					= ${provisioning.debug.cert.ios}
cert.ios.DEBUG.passphrase		= ${provisioning.debug.passphrase}

provisioning_profile.QA			= ${provisioning.debug.profile}
cert.ios.QA						= ${provisioning.debug.cert.ios}
cert.ios.QA.passphrase			= ${provisioning.debug.passphrase}

provisioning_profile.QA-Lite	= ${provisioning.debug.profile}
cert.ios.QA-Lite				= ${provisioning.debug.cert.ios}
cert.ios.QA-Lite.passphrase		= ${provisioning.debug.passphrase}


# Desktop
#
provisioning_profile.DESKTOP-Integration	= ${provisioning.debug.profile}
cert.DESKTOP-Integration					= ${provisioning.debug.cert.selfSigned}
cert.DESKTOP-Integration.passphrase			= ${provisioning.debug.selfSigned.passphrase}

provisioning_profile.DESKTOP-Marketing		= ${provisioning.debug.profile}
cert.DESKTOP-Marketing						= ${provisioning.debug.cert.selfSigned}
cert.DESKTOP-Marketing.passphrase			= ${provisioning.debug.selfSigned.passphrase}






##
# EXTENSIONS (see /ane-<platform> directory)
##

extensions.apple = <extensionID>com.milkmangames.extensions.StoreKit</extensionID><extensionID>com.milkmangames.extensions.RateBox</extensionID><extensionID>pl.mateuszmackowiak.nativeANE.NativeDialogs</extensionID><extensionID>com.sticksports.nativeExtensions.InAppPurchase</extensionID>
extensions.google = <extensionID>pl.mateuszmackowiak.nativeANE.NativeDialogs</extensionID>
extensions.desktop =
